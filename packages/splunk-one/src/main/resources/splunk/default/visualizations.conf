# Custom visualization: fixed-domain single value + sparkline.
# Folder: appserver/static/visualizations/fixed_single_value/

[fixed_single_value]
label = SplunkStuff — Fixed single value (AMD)
description = Single value with delta and fixed min/max sparkline. Expects column-major results with at least one numeric field (last numeric column is used for the series).
default_height = 200
search_fragment = | timechart span=1h avg(my_metric) as value
supports_drilldown = 0
supports_trellis = 0

# Tiny starter visualization: compact latest-value tile.
# Folder: appserver/static/visualizations/simple_small_viz/

[simple_small_viz]
label = SplunkStuff — Simple small viz
description = Minimal compact tile that shows the latest value from the first numeric field.
default_height = 120
search_fragment = | makeresults count=5 | streamstats count as value
supports_drilldown = 0
supports_trellis = 0

# Vanilla AMD: single value + footer SVG line with fixed Y min/max (no filled sparkband).
# Folder: appserver/static/visualizations/line_single_value/

[line_single_value]
label = SplunkStuff — Line single value (AMD)
description = Single value with delta and a footer line chart; fixed vertical scale via formatter min/max. Expects column-major results with at least one numeric column (last all-numeric column drives the series).
default_height = 200
search_fragment = | timechart span=1h avg(my_metric) as value
supports_drilldown = 0
supports_trellis = 0

# React-bundled variant (same UX intent; implementation mounts React into the viz container).
# Folder: appserver/static/visualizations/fixed_single_value_react/
[fixed_single_value_react]
label = SplunkStuff — Fixed single value (React bundle)
description = React-based single value with delta and fixed min/max sparkline. Expects column-major results with at least one numeric field (last numeric column is used for the series).
default_height = 200
search_fragment = | timechart span=1h avg(my_metric) as value
supports_drilldown = 0
supports_trellis = 0

# React LineChart demo parity (majors + footer line path, thresholds, comparison, anomalies).
# Folder: appserver/static/visualizations/fixed_loaded_line/

[fixed_loaded_line]
label = SplunkStuff — Fixed loaded line (React LineChart)
description = React LineChart single-value tile with delta and footer SVG line; fixed Y min/max; optional comparison overlay, thresholds, SMA, downsampling, anomalies, and drilldown. Uses _time when present and the last all-numeric non-_time column for the series.
default_height = 200
search_fragment = | timechart span=1h avg(my_metric) as value
supports_drilldown = 0
supports_trellis = 0

# Readable vanilla AMD (hand-maintained visualization.js; not Webpack).
# Folder: appserver/static/visualizations/fixed_loaded_line_vanilla/

[fixed_loaded_line_vanilla]
label = SplunkStuff — Fixed loaded line (vanilla AMD)
description = Readable AMD single-value + line chart; same formatter options as React fixed_loaded_line.
default_height = 200
search_fragment = | timechart span=1h avg(my_metric) as value
supports_drilldown = 0
supports_trellis = 0

# Readable vanilla AMD — full LineChart-style formatter (hand-maintained visualization.js).
# Folder: appserver/static/visualizations/splunkstuff_kpi_line/

[splunkstuff_kpi_line]
label = SplunkStuff — KPI loaded line (vanilla AMD, full options)
description = Readable AMD single-value + line chart; LineChart-style layout, padding, stroke, threshold styling, and hover annotation options.
default_height = 200
search_fragment = | timechart span=1h avg(my_metric) as value
supports_drilldown = 0
supports_trellis = 0

# Verbose twin: same behavior, documented source + SPLUNKSTUFF_KPI_LINE_VERBOSE_DEBUG logging.
# Folder: appserver/static/visualizations/splunkstuff_kpi_line_verbose/

[splunkstuff_kpi_line_verbose]
label = SplunkStuff — KPI loaded line verbose (documented AMD)
description = Same as splunkstuff_kpi_line with expanded comments; set window.SPLUNKSTUFF_KPI_LINE_VERBOSE_DEBUG=true for effectiveConfig logs.
default_height = 200
search_fragment = | timechart span=1h avg(my_metric) as value
supports_drilldown = 0
supports_trellis = 0

# Exact clone of splunkstuff_kpi_line_verbose (manual refactor copy).
# Folder: appserver/static/visualizations/refactor_viz_manual/

[refactor_viz_manual]
label = SplunkStuff — Refactor viz manual (clone of KPI verbose)
description = Same UX as splunkstuff_kpi_line_verbose; AMD + per-viz splunkstuff*.js (synced from repo _shared). REFACTOR_VIZ_MANUAL_DEBUG for console traces.
default_height = 200
search_fragment = | timechart span=1h avg(my_metric) as value
supports_drilldown = 0
supports_trellis = 0

# Categorical pie with Top N + Other bucket.
# Folder: appserver/static/visualizations/splunkstuff_pie_chart/

[splunkstuff_pie_chart]
label = SplunkStuff — Data pie (Top N)
description = Categorical pie chart with Top N + Other bucket. Expects a label column and a numeric column (e.g. stats count by field).
default_height = 220
search_fragment = | stats count by category_field
supports_drilldown = 0
supports_trellis = 0

# Single value + sparkline with extended formatter.
# Folder: appserver/static/visualizations/splunkstuff_sparkline_value/

[splunkstuff_sparkline_value]
label = SplunkStuff — Value + sparkline (extended)
description = Single value with delta and footer sparkline; extended formatter for scale, trend, targets, and thresholds. Expects _time and a numeric column.
default_height = 280
search_fragment = | timechart span=1h avg(my_metric) as value
supports_drilldown = 0
supports_trellis = 0

# KPI sparkline with always-visible labels (gallery intended demo).
# Folder: appserver/static/visualizations/splunkstuff_kpi_sparkline/

[splunkstuff_kpi_sparkline]
label = SplunkStuff — KPI + sparkline (labels demo)
description = Single value with Current/Change labels, Demo KPI badge, hover, and spark point labels. Use for gallery demo panels.
default_height = 280
search_fragment = | timechart span=1h avg(my_metric) as value
supports_drilldown = 0
supports_trellis = 0

# Splunk 9.4 custom viz tutorial: radial meter (D3 tutorial ported to vanilla AMD SVG).
# Folder: appserver/static/visualizations/radial_meter/

[radial_meter]
label = SplunkStuff — Radial meter (Splunk tutorial)
description = Radial gauge for a single numeric value. Tutorial parity: partial arc dial, center readout, mainColor and maxValue formatter options.
default_height = 220
search_fragment = | stats count
supports_drilldown = 0
supports_trellis = 0

# Showcase copies — forced demo defaults for gallery row 5.

[splunkstuff_pie_showcase]
label = SplunkStuff — Data pie showcase (Top N demo)
description = Pie chart showcase with baked-in Top 5 + Other demo settings for the custom viz gallery.
default_height = 220
search_fragment = | stats count by category_field
supports_drilldown = 0
supports_trellis = 0

[splunkstuff_sparkline_showcase]
label = SplunkStuff — Value + sparkline showcase
description = Sparkline KPI showcase with baked-in labels, badge, hover, and point labels for the custom viz gallery.
default_height = 280
search_fragment = | timechart span=1h avg(my_metric) as value
supports_drilldown = 0
supports_trellis = 0

[radial_meter_showcase]
label = SplunkStuff — Radial meter showcase
description = Radial meter showcase with baked-in Splunk 9.4 tutorial dial settings for the custom viz gallery.
default_height = 220
search_fragment = | stats count
supports_drilldown = 0
supports_trellis = 0
